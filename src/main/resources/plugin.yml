name: "${pluginName}"
prefix: "${pluginPrefix}"
description: "${pluginDescription}"
website: "${pluginWebsite}"

main: "${pluginMainClass}"
version: "${pluginVersion}"
api-version: "1.13"

authors:
  - "SirBlobman"

depend:
  - "ShopGUIPlus"
  - "UltimateStacker"
